# اصلاح دسته‌بندی گراف Research Atlas

- `assets/js/atlas-classifier.js`: طبقه‌بندی چندبرچسبی بر اساس **عنوان + چکیدهٔ موجود**، به‌همراه موارد بازبینی‌شده؛ هیچ مقالهٔ ناشناخته‌ای به‌طور پیش‌فرض به Graph وصل نمی‌شود.
- `assets/js/about-atlas.js`: از طبقه‌بندی جدید برای ساخت یال‌ها استفاده می‌کند و در tooltip مشخص می‌کند دسته‌بندی با `title only` بوده یا `title + abstract`.
- `assets/data/publication-abstracts.json`: حافظهٔ محلی چکیده‌های تاییدشده؛ در نسخهٔ اولیه خالی است، بنابراین ادعای استفاده از چکیده برای مقاله‌های بدون متن نداریم.
- `tools/update_abstracts.py`: در صورت داشتن اینترنت و Node/Python با دستور `python tools/update_abstracts.py` چکیده‌های در دسترس را **با تطبیق دقیق DOI** از OpenAlex دریافت و cache می‌کند. برای کارکرد صفحه نیازی به اجرای این اسکریپت نیست. مقالات بدون DOI، فاقد چکیدهٔ OpenAlex، یا با عدم دسترسی، عنوان‌محور باقی می‌مانند.

برای تکمیل دستیِ دقیق‌تر، به هر مدخل در `assets/js/site-data.js` می‌توان فیلد `abstract: "متن واقعی چکیده"` اضافه کرد؛ این فیلد نسبت به کش ارجحیت دارد. در مورد مقاله‌های جدید، پس از دریافت چکیده، صفحه را بازبارگذاری کنید. برای گسترش بررسی انسانی، آرایهٔ `REVIEWED` را در فایل classifier ویرایش کنید.

**نمونهٔ اصلاح:** مقالهٔ Iterative Pseudo-Sparse Partial Least Square به Biomedical وصل شده؛ صرف وجود Higher-Order در عنوان، آن را به Higher-Order Graph & Topology وصل نمی‌کند. حوزهٔ AI & Signal Processing کلی‌تر فعلاً یک نود مستقل در این طرح شش‌حوزه‌ای ندارد. دسته‌بندی صرفاً پیشنهادی و شفاف است و جایگزین ارزیابی علمی انسانی نیست.

نکته: این تغییر دریافت خودکار «مقالهٔ جدید» از Google Scholar را اضافه نمی‌کند؛ گراف همچنان مقاله‌ها را از `site-data.js` می‌خواند. اسکریپت صرفاً چکیدهٔ مقاله‌های موجود را به‌روزرسانی می‌کند.
